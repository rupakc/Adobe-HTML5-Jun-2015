window.localStorage
    - key/value store
    - Both key and value should be strings
    - 10MB
API
    - setItem(key, value)
    - getItem(key) //=> value
    - removeItem(key)
    - key(index) //=> key at index
    - clear()
    - length
    
Security
    - Same Origin Policy
    
